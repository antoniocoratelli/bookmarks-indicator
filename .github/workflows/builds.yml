name: builds

on: 
  push:
    branches:
    - master

jobs:
  
  build-debian-9:
    runs-on: ubuntu-latest

    steps:
    - uses: docker://debian:9
    - run: sudo apt install build-essential cmake libgtk-3-dev libgtkmm-3.0-dev
    - run: mkdir -p bookmarks-indicator/build
    - run: cd bookmarks-indicator/build
    - run: cmake .. && make && sudo make install 
